Removed/Archived assets
------------------------

The following unused assets were cleaned from the `pwa/` folder to reduce noise for reviewers:

- Removed: `src/assets/react.svg`
- Removed: `src/assets/vite.svg`
- Removed: `public/icons.svg`

Note: `src/assets/hero.png` remained in place (binary file) due to tool limitations; consider removing or replacing it if not needed.

If you want these files restored, they can be recovered from your local Git history.
